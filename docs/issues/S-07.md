## 사용자 스토리

> 나는 **만든 걸 인터넷 주소로 열어보고 싶다.**
> 그래야 남에게 보여줄 수 있고, 진짜 만들었다는 감각이 생긴다.

## 완료 조건 (AC)

- [ ] GitHub 원격 저장소 생성 및 `main` 브랜치 푸시
- [ ] GitHub Pages 활성화 (Settings → Pages → Deploy from branch → `main` / root)
- [ ] 공개 URL 접속해서 계산기 정상 동작 확인
- [ ] **휴대폰에서 직접 열어보기**
- [ ] `README.md`에 프로젝트 설명 3줄 + 배포 URL
- [ ] 스프린트 리뷰: URL을 실제 사람 1명 이상에게 보내고 반응 기록

## 로컬 확인 방법

`src/tax-rules.js`를 ES 모듈로 import하므로 `file://`로 열면 CORS 오류가 난다.
정적 서버를 띄워서 확인할 것:

```bash
npx serve .
```

## 배우는 것

git 원격 저장소, 배포 파이프라인, "내 컴퓨터에서만 되는 것"과 "배포된 것"의 차이

## 의존

- Blocked by #5

---
예상 3h · Must · Sprint 1
