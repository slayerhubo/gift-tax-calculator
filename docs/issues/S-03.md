## 사용자 스토리

> 나는 **금액을 넣으면 납부세액이 나오는 함수**를 만들고 싶다.
> 그래야 화면과 무관하게 계산 자체를 검증할 수 있다.

## 완료 조건 (AC)

- [ ] `src/tax-calculator.js`에 `calculateGiftTax(증여재산가액)` 구현
- [ ] 반환값에 계산 **전 단계**가 담길 것:
  ```js
  {
    증여재산가액, 증여재산공제, 과세표준,
    적용구간,        // 예: '1억원 초과 ~ 5억원 이하'
    산출세액, 신고세액공제, 납부할세액
  }
  ```
- [ ] 세율·공제 값은 **전부 `src/tax-rules.js`에서 import** — 함수 안에 숫자를 직접 쓰지 않는다
- [ ] 누진공제 방식 사용: `과세표준 × rate - progressiveDeduction`
- [ ] 공제액 ≥ 증여가액이면 과세표준 0 → 납부세액 0
- [ ] 신고세액공제(`FILING_TAX_CREDIT_RATE`) 적용
- [ ] **DOM을 전혀 건드리지 않는 순수 함수** (S-04 테스트를 위해 필수)
- [ ] 절사 단위(`ROUNDING_UNIT`) 확정하고 주석으로 근거 남기기

## 의존

- Blocked by #1 (완료됨 — `src/tax-rules.js` 사용)

## 배우는 것

순수 함수, 관심사 분리, 누진세 계산 로직

---
예상 4h · Must · Sprint 1
