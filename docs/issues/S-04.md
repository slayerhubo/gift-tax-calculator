## 사용자 스토리

> 나는 **계산이 맞는지 자동으로 검증**하고 싶다.
> 그래야 나중에 코드를 고칠 때 안심할 수 있다.

## 완료 조건 (AC)

- [ ] `tests/tax-calculator.test.js` 작성 — **Node 내장 테스트 러너** 사용 (`node:test` + `node:assert/strict`)
- [ ] `npm test` 로 실행되고 전부 통과
- [ ] **세율 구간 경계값 ±1원** 테스트 — 1억 / 5억 / 10억 / 30억
- [ ] 0원 / 공제 이하 금액(5천만원 이하) / 최고구간 초과 금액 테스트
- [ ] **국세청 자동계산 서비스 결과와 최소 2건 대조**하고 결과를 주석으로 기록
- [ ] 대조 과정에서 절사 단위(`ROUNDING_UNIT`) 확정 → `src/tax-rules.js` 반영

## 실행 방법

```bash
npm test
```

외부 라이브러리 설치 불필요 — Node 24 내장 기능만 쓴다 (`npm install` 할 것 없음).

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { calculateGiftTax } from '../src/tax-calculator.js';

test('과세표준 1억원 경계', () => {
  assert.equal(calculateGiftTax(150_000_000).산출세액, 10_000_000);
});
```

## 참고: 손계산 예시 (직계존속→성년 자녀, 기한 내 신고)

증여 3억원 → 공제 5,000만 → 과세표준 2.5억
→ 산출세액 `2.5억 × 20% - 1,000만 = 4,000만`
→ 신고세액공제 `4,000만 × 3% = 120만`
→ **납부할 세액 3,880만원**

*이 값이 맞는지 국세청 자동계산 서비스로 반드시 검증할 것.*

## 배우는 것

**경계값 테스트 사고방식.** 버그는 거의 항상 구간 경계에서 난다.
이번 스프린트에서 학습 밀도가 가장 높은 티켓.

## 의존

- Blocked by #S-03

---
예상 2.5h · Must · Sprint 1
